#!/bin/bash
# Skill Finder environment configuration

# Get the directory where this .env file is located
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Source local environment overrides if they exist
if [ -f "$SKILL_DIR/.env.local" ]; then
    source "$SKILL_DIR/.env.local"
fi
